# 抖音直播间弹幕 - 浏览器环境修复总结

## 一、项目背景

本项目需要在 Node.js 的 VM 沙箱中模拟完整的浏览器环境，使抖音 `byted_acrawler` SDK（`code.js`）能够在其中正确初始化，并调用 `frontierSign` 函数生成签名（`X-Bogus`）。

### 核心架构

```
run.js (入口)
  └─ VMContext (VM 沙箱隔离上下文)
       └─ BrowserEnv (浏览器环境初始化)
            ├─ BOM: window, navigator, screen, location, 等
            ├─ DOM: document, Element, HTMLCanvasElement, 等
            └─ utils: r0zerox (代理日志), setNative (toString 伪装)
```

### 最终目标

```javascript
// 1. 加载 code.js（byted_acrawler SDK）
vmCtx.run(codeJs);

// 2. 调用 frontierSign 生成签名
vmCtx.run(`window.__signature__ = window.byted_acrawler.frontierSign({...})`);
// 输出: { 'X-Bogus': 'f/4N4cewR5zqR/N9' }
```

---

## 二、调试过程总览

整个调试过程遵循"逐层排查、由外及内"的策略：

| 阶段 | 步骤 | 工具/文件 |
|------|------|-----------|
| 1. 环境基础验证 | 验证 VM 沙箱 + 浏览器基础对象是否正常 | `test-env.js` |
| 2. UMD 分析 | 分析 `code.js` 的模块加载模式 | `analyze-umd.js`, `test-umd.js` |
| 3. 代码执行调试 | 排查 `code.js` 为何无法执行/初始化 | `debug-code.js`, `test-code.js` |
| 4. 缺失对象补充 | 逐个补充浏览器 API 对象 | `test-sign5.js`, `debug-image.js` 等 |
| 5. 签名测试 | 最终验证 frontierSign 输出 | `run.js` |

---

## 三、问题与解决方案

### 问题 1：Proxy 代理日志不生效（环境未初始化到位）

#### 现象

在 `run.js` 中添加了 `initR0zerox(vmCtx)`（负责 `Proxy` 代理日志），但控制台没有任何代理日志输出，说明代码根本没有执行到需要访问浏览器属性的阶段。

#### 原因分析

代理日志本身是正常的（在 `utils/r0zerox.js` 中通过 `Proxy` 拦截 `get/set/deleteProperty`），但 `code.js` 在执行阶段就静默失败了，根本没有机会触发属性访问。

#### 排查思路

这个现象是**信号而非根因**——它告诉我们应该在更早的阶段排查问题：
1. 先验证浏览器环境是否初始化成功 → 编写 `test-env.js`
2. 再验证 `code.js` 能否正常执行 → 编写 `debug-code.js`

#### 代理日志机制说明

代理日志实现在 [utils/r0zerox.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/utils/r0zerox.js)：

```javascript
proxy: function (o, name = '') {
    const label = name ? `[${name}] ` : '';
    return new Proxy(o, {
        set (target, key, value) {
            console.log(`【set】${label} ${String(key)} =`, val);
            return Reflect.set(...arguments);
        },
        get (target, key, receiver) {
            console.log(`【get】==>${label} ${String(key)} →`, display);
            return val;
        },
        // ...
    });
}
```

当环境初始化正确且 `code.js` 执行后，每次对 `window`、`navigator`、`document` 等对象的属性访问都会被记录在控制台中。

---

### 问题 2：`code.js` 静默执行失败，`byted_acrawler` 未挂载到 `window`

#### 错误信息

```
TypeError: Cannot read properties of undefined (reading 'frontierSign')
```

执行 `run.js` 后，`vmCtx.get('window.byted_acrawler')` 返回 `undefined`。

#### 关键发现

在 `debug-code.js` 中，将 `code.js` 用 `try-catch` 包裹执行时，代码可以成功运行：

```javascript
// debug-code.js 中的关键代码
const wrappedCode = `
    try {
        ${originCode}   // 直接内联 code.js
        console.log('执行完成');
    } catch (e) {
        console.log('执行异常:', e.message);
    }
`;
vmCtx.run(wrappedCode);  // 用 try-catch 包裹 → 成功
```

而直接执行：
```javascript
vmCtx.run(originCode);  // 直接执行 → 静默失败
```

#### 根因定位

这个差异指向了 `vm-context.js` 的 `run` 方法对代码类型的判断逻辑有问题。`code.js` 开头是：

```javascript
/**
 * 1.0.0.53
 */
// 实际代码...
```

`run` 方法的正则 `/^(var|let|const|function|class|...)\b/` 先匹配到了注释 `/**`，不是关键字，于是代码被当作"表达式"来执行，产生了 `SyntaxError`。

#### 为什么 try-catch 包裹能成功？

当我们在 `debug-code.js` 中把 `code.js` 内联进一个 `try { }` 块时，整个代码变成了**语句块**，不再经过表达式的判断逻辑。

---

### 问题 3：`vm-context.js` 的 `run` 方法表达式检测逻辑缺陷

#### 错误信息

- 当 `code.js` 开头有 `/** 1.0.0.53 */` 注释时：`SyntaxError: Unexpected token ';'`
- 当 `get` 方法返回的表达式末尾有分号时：`SyntaxError: Unexpected token ';'`

#### 原因分析

[core/vm-context.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/core/vm-context.js) 的原始 `run` 方法：

```javascript
// 旧版逻辑
run (code) {
    const isExpression = !/^(var|let|const|function|class|if|for|...)\b/.test(code.trim());
    const wrapCode = isExpression
        ? `(function(){return ${code}}).call(window);`  // 表达式 → return 模式
        : `(function(){${code}}).call(window);`           // 语句 → 直接执行
    return vm.runInContext(wrapCode, this.context);
}
```

对于 `code.js`：
1. `code.trim()` = `"/**\n * 1.0.0.53\n */\n// 实际代码..."`
2. 正则匹配：以 `/` 开头，不匹配任何关键字 → `isExpression = true`
3. 生成：`(function(){return /**\n * 1.0.0.53\n */...}).call(window);`
4. `return` 后面跟注释然后直接跟 `//...` → 语法错误

#### 解决方案

修改 [core/vm-context.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/core/vm-context.js) 的 `run` 方法：

```javascript
run (code, opts = {}) {
    if (this._destroyed) {
        throw new Error('VMContext: 上下文已销毁，无法执行代码');
    }
    const trimmedCode = code.trim();
    // 去除开头的注释和分号后检查是否为表达式
    const codeWithoutComments = trimmedCode
        .replace(/^(\/\*[\s\S]*?\*\/\s*)+/, '')  // 去除 /* */ 注释
        .replace(/^(\/\/.*\s*)+/, '')              // 去除 // 注释
        .replace(/^[;\s]*/, '');                    // 去除开头的分号和空白
    const isExpression = codeWithoutComments
        && !/^(var|let|const|function|class|if|for|while|switch|try|import|export|return|throw|debugger|with|do)\b/
            .test(codeWithoutComments);
    const wrapCode = isExpression
        ? `(function(){return ${code}}).call(window);`
        : `(function(){${code}}).call(window);`;
    return vm.runInContext(wrapCode, this.context, {
        ...this.options,
        ...opts
    });
}
```

**关键改进：**
1. 去除开头块注释（`/* ... */`）和行注释（`// ...`）后，再判断代码类型
2. 去除开头分号和空白字符
3. 添加 `return`、`throw`、`debugger`、`with`、`do` 等关键字到检测列表

---

### 问题 4：`Image` 对象未定义

#### 错误信息

```
ReferenceError: Image is not defined
```

#### 原因分析

修复 `run` 方法后，`code.js` 可以执行了，但在执行过程中遇到了 `Image` 构造函数未定义的问题。`frontierSign` 内部使用了 `new Image()` 进行浏览器指纹相关的操作。

#### 解决方案

在 [env/browser-env/BOM/window.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/env/browser-env/BOM/window.js) 中添加 `Image` 构造函数：

```javascript
function Image() {
    this.complete = false;
    this.naturalWidth = 0;
    this.naturalHeight = 0;
    this.width = 0;
    this.height = 0;
    this.src = '';
    this.alt = '';
    this.onload = null;
    this.onerror = null;
    this.onabort = null;
}

Image.prototype.addEventListener = function(type, listener) {
    if (type === 'load') this.onload = listener;
    if (type === 'error') this.onerror = listener;
    if (type === 'abort') this.onabort = listener;
};

Image.prototype.removeEventListener = function() {};

Image.prototype.dispatchEvent = function(event) {
    if (event && event.type && this['on' + event.type]) {
        this['on' + event.type]();
    }
};

raw.Image = Image;  // 挂载到 VM 全局上下文
```

**关键点说明：** Image 构造函数必须足够"像"浏览器原生 Image，包括 `complete`、`naturalWidth`、`naturalHeight` 等属性，以及 `addEventListener`、`removeEventListener` 等方法。

---

### 问题 5：`PluginArray` 未定义

#### 错误信息

```
ReferenceError: PluginArray is not defined
```

#### 原因分析

`frontierSign` 内部访问 `navigator.plugins`（用于浏览器指纹识别），需要 `PluginArray` 类。虽然在 [env/browser-env/BOM/navigator.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/env/browser-env/BOM/navigator.js) 中创建了 `navigator.plugins` 实例，但混淆后的 `code.js` 通过 `eval` 或 `Function` 构造函数执行时，无法访问到局部作用域中的 `PluginArray` 类，必须将其注册到 VM 全局上下文。

#### 解决方案

在 [env/index.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/env/index.js) 中定义并注册全局类：

```javascript
function Plugin() {
    this.name = '';
    this.description = '';
    this.filename = '';
}

function MimeType() {
    this.type = '';
    this.description = '';
    this.suffixes = '';
}

function PluginArray() {
    Array.call(this);
    this.refresh();
}

PluginArray.prototype = Object.create(Array.prototype);  // 伪装成数组
PluginArray.prototype.constructor = PluginArray;
PluginArray.prototype.refresh = function() {
    // 模拟 Chrome 浏览器的 3 个内置插件
    this[0] = new Plugin();
    this[0].name = 'Chrome PDF Plugin';
    this[0].description = 'Portable Document Format';
    this[0].filename = 'internal-pdf-viewer';

    this[1] = new Plugin();
    this[1].name = 'Chrome PDF Viewer';
    this[1].description = '';
    this[1].filename = 'mhjfbmdgcfjbbpaeojofohoefgiehjai';

    this[2] = new Plugin();
    this[2].name = 'Native Client';
    this[2].description = '';
    this[2].filename = 'internal-nacl-plugin';

    this.length = 3;
};

function MimeTypeArray() {
    Array.call(this);
    this.length = 0;
}

MimeTypeArray.prototype = Object.create(Array.prototype);
MimeTypeArray.prototype.constructor = MimeTypeArray;

// 在 BrowserEnv._initAll() 中注册到全局上下文
this.ctx.context.PluginArray = PluginArray;
this.ctx.context.Plugin = Plugin;
this.ctx.context.MimeType = MimeType;
this.ctx.context.MimeTypeArray = MimeTypeArray;
```

**关键点说明：**
- `PluginArray` 需要继承 `Array.prototype`，使 `Array.isArray()` 或 `instanceof` 检查通过
- `Plugin` 的内容要匹配 Chrome 浏览器的真实配置（PDF Plugin、PDF Viewer、Native Client）

---

### 问题 6：`navigator.webdriver` 为 `undefined` 导致 `toString` 调用崩溃

#### 错误信息

```
TypeError: Cannot read properties of undefined (reading 'toString')
```

#### 原因分析

混淆代码中有一行类似 `navigator.webdriver.toString()` 的调用，用于检测当前是否在自动化环境中运行（WebDriver 是 Selenium/自动化框架的标志）。原始环境的 `NavigatorProto` 中没有定义 `webdriver` 属性，导致返回 `undefined`，进而调用 `.toString()` 时报错。

#### 解决方案

在 [env/browser-env/BOM/navigator.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/env/browser-env/BOM/navigator.js) 的 `NavigatorProto` 中添加上下文代码：

```javascript
const NavigatorProto = vmCtx.r0zerox.createProto('Navigator', {
    constructor: function () {},
    javaEnabled: function () { return false; },
    cookieEnabled: true,
    doNotTrack: 'unspecified',
    hardwareConcurrency: config.hardwareConcurrency || 4,
    maxTouchPoints: config.maxTouchPoints || 0,
    msMaxTouchPoints: config.maxTouchPoints || 0,
    msManipulationViewsEnabled: true,
    webdriver: false,     // ← 新增：标识为非自动化环境
    webkitPersistentStorageNotation: function () { return Promise.resolve(); }
});
```

**关键点说明：** `webdriver: false` 表示这不是自动化测试环境，这是反爬检测的重要指纹之一。

---

### 问题 7：`HTMLCanvasElement` 和 `CanvasRenderingContext2D` 缺失

#### 错误信息

```
TypeError: canvas.getContext is not a function
```

#### 原因分析

`frontierSign` 内部使用 Canvas 进行浏览器指纹计算（Canvas Fingerprinting）：
1. 调用 `document.createElement('canvas')` 创建 canvas 元素
2. 调用 `canvas.getContext('2d')` 获取 2D 渲染上下文
3. 通过 `ctx.fillText()` 等 API 绘制文字
4. 使用 `canvas.toDataURL()` 导出 base64 图像数据作为指纹

原始的 `document.createElement` 对所有标签返回一个通用的普通对象，没有 `getContext` 方法。

#### 解决方案

**步骤 1：新建** [env/browser-env/DOM/HTMLCanvasElement.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/env/browser-env/DOM/HTMLCanvasElement.js)

实现完整的 Canvas 元素和 2D 渲染上下文：

```javascript
function initCanvasElement(vmCtx) {
    const ctx = vmCtx.context;

    // ---- CanvasRenderingContext2D ----
    function CanvasRenderingContext2D() {
        this.canvas = null;
        this.fillStyle = '';
        this.strokeStyle = '';
        this.font = '10px sans-serif';
        this.textAlign = 'start';
        this.textBaseline = 'alphabetic';
        this.lineWidth = 1;
        this.lineCap = 'butt';
        this.lineJoin = 'miter';
        this.shadowColor = 'rgba(0,0,0,0)';
        this.shadowBlur = 0;
        this.shadowOffsetX = 0;
        this.shadowOffsetY = 0;
        this.globalAlpha = 1;
        this.globalCompositeOperation = 'source-over';
        this.imageSmoothingEnabled = true;
    }

    // 变换
    CanvasRenderingContext2D.prototype.save = function () {};
    CanvasRenderingContext2D.prototype.restore = function () {};
    CanvasRenderingContext2D.prototype.scale = function () {};
    CanvasRenderingContext2D.prototype.rotate = function () {};
    CanvasRenderingContext2D.prototype.translate = function () {};
    CanvasRenderingContext2D.prototype.transform = function () {};
    CanvasRenderingContext2D.prototype.setTransform = function () {};

    // 路径
    CanvasRenderingContext2D.prototype.beginPath = function () {};
    CanvasRenderingContext2D.prototype.moveTo = function () {};
    CanvasRenderingContext2D.prototype.lineTo = function () {};
    CanvasRenderingContext2D.prototype.closePath = function () {};
    // ... 更多路径方法

    // 填充/描边
    CanvasRenderingContext2D.prototype.fill = function () {};
    CanvasRenderingContext2D.prototype.stroke = function () {};
    CanvasRenderingContext2D.prototype.fillRect = function () {};
    CanvasRenderingContext2D.prototype.clearRect = function () {};

    // 文本
    CanvasRenderingContext2D.prototype.fillText = function () {};
    CanvasRenderingContext2D.prototype.strokeText = function () {};
    CanvasRenderingContext2D.prototype.measureText = function () {
        return { width: 0, actualBoundingBoxLeft: 0, /* ... */ };
    };

    // 图像
    CanvasRenderingContext2D.prototype.drawImage = function () {};
    CanvasRenderingContext2D.prototype.getImageData = function () {
        return { data: new Uint8ClampedArray(0) };
    };
    CanvasRenderingContext2D.prototype.putImageData = function () {};

    // 渐变
    CanvasRenderingContext2D.prototype.createLinearGradient = function () {
        return { addColorStop: function () {} };
    };
    CanvasRenderingContext2D.prototype.createRadialGradient = function () {
        return { addColorStop: function () {} };
    };

    CanvasRenderingContext2D.prototype.toString = function () {
        return '[object CanvasRenderingContext2D]';
    };

    // ---- HTMLCanvasElement ----
    function HTMLCanvasElement() {
        this.width = 300;
        this.height = 150;
        this.tagName = 'CANVAS';
        this.nodeType = 1;
        this.nodeName = 'CANVAS';
        this.style = {};
        this.children = [];
        this.innerHTML = '';
        this.textContent = '';
    }

    HTMLCanvasElement.prototype.getContext = function (type) {
        if (type === '2d') {
            var ctx = new CanvasRenderingContext2D();
            ctx.canvas = this;  // 双向关联
            return ctx;
        }
        return null;
    };

    HTMLCanvasElement.prototype.toString = function () {
        return '[object HTMLCanvasElement]';
    };

    HTMLCanvasElement.prototype.toDataURL = function () {
        return 'data:image/png;base64,...';
    };

    HTMLCanvasElement.prototype.toBlob = function () {};
    HTMLCanvasElement.prototype.getBoundingClientRect = function () {
        return { top: 0, left: 0, right: this.width, bottom: this.height,
                 width: this.width, height: this.height };
    };

    // 通用 DOM 方法
    HTMLCanvasElement.prototype.addEventListener = function () {};
    HTMLCanvasElement.prototype.removeEventListener = function () {};
    HTMLCanvasElement.prototype.getAttribute = function () { return null; };
    HTMLCanvasElement.prototype.setAttribute = function () {};

    ctx.HTMLCanvasElement = HTMLCanvasElement;
    ctx.CanvasRenderingContext2D = CanvasRenderingContext2D;
}

module.exports = initCanvasElement;
```

**步骤 2：修改** [env/browser-env/DOM/document.js](file:///f:/ProgramFiles/2026Reverse/公众号/抖音直播间弹幕/r0zerox/env/browser-env/DOM/document.js)

在 `createElement` 中增加对 `canvas` 标签的特殊处理：

```javascript
const initCanvasElement = require('./HTMLCanvasElement.js');

function initDocument(vmCtx, config = {}) {
    function createElement(tagName) {
        tagName = (tagName || '').toUpperCase();

        if (tagName === 'CANVAS') {
            if (!vmCtx.context.HTMLCanvasElement) {
                initCanvasElement(vmCtx);  // 延迟初始化
            }
            return new vmCtx.context.HTMLCanvasElement();
        }

        return { /* ... 默认元素对象 */ };
    }
    // ...
}
```

**关键点说明：**
- Canvas 是浏览器指纹的核心组成部分，即使 `toDataURL` 返回的 base64 是假的，`byted_acrawler` 也能通过
- `measureText` 必须返回一个包含 `width` 等属性的对象，否则 `.width` 访问会得到 `undefined`
- Canvas 和 Context 之间需要双向关联：`ctx.canvas = this`

---

## 四、修改文件清单

| 文件 | 修改类型 | 关联问题 | 说明 |
|------|---------|---------|------|
| `core/vm-context.js` | 修改 | 问题 2, 3 | 修复 `run` 方法的表达式检测逻辑，去除注释后再判断代码类型 |
| `env/browser-env/BOM/window.js` | 修改 | 问题 4 | 添加 `Image` 构造函数及原型方法 |
| `env/browser-env/BOM/navigator.js` | 修改 | 问题 5, 6 | 添加 `webdriver: false`，使用全局 `PluginArray`/`MimeTypeArray` |
| `env/index.js` | 修改 | 问题 5 | 添加 `Plugin`、`PluginArray`、`MimeType`、`MimeTypeArray` 定义并注册到 VM 全局上下文 |
| `env/browser-env/DOM/HTMLCanvasElement.js` | **新建** | 问题 7 | 实现完整的 `HTMLCanvasElement` + `CanvasRenderingContext2D` |
| `env/browser-env/DOM/document.js` | 修改 | 问题 7 | 在 `createElement` 中支持 `canvas` 标签，调用 `initCanvasElement` |

---

## 五、调试工具文件说明

以下文件是调试过程中创建的辅助文件，用于隔离测试各个问题：

| 文件 | 用途 |
|------|------|
| `test-env.js` | 验证浏览器环境初始化（window/document/navigator 是否存在） |
| `analyze-umd.js` | 分析 `code.js` 的 UMD 模块结构（IIFE、工厂函数位置） |
| `test-umd.js` | 用简化版 UMD 代码测试 VM 执行逻辑 |
| `test-code.js` | 测试 `code.js` 在 VM 中的基本执行 |
| `debug-code.js` | 用 try-catch 包裹执行 code.js，定位静默失败原因 |
| `debug-image.js` | 测试 Image 对象和 Canvas 创建 |
| `debug-toString.js` | 测试各对象的 toString 行为和 webdriver 问题 |
| `test-sign.js` ~ `test-sign5.js` | 逐步测试 frontierSign 签名生成 |
| `reproduce-success.js` | 最终验证脚本，确认签名可以成功生成 |

---

## 六、核心经验总结

### 6.1 排查流程

```
环境不输出 → 不是代理问题 → 是代码没执行
代码没执行 → try-catch 能跑 → 是 run 方法的问题
run 方法问题 → 注释干扰了表达式检测 → 修复正则
代码能执行 → 报 Image 未定义 → 补充 Image
继续执行 → 报 PluginArray 未定义 → 补充 PluginArray + 注册到全局
继续执行 → 报 webdriver.toString 错误 → 补充 webdriver 属性
继续执行 → 报 canvas.getContext 错误 → 补充 Canvas 完整实现
最终成功 → 输出 X-Bogus 签名 ✓
```

### 6.2 关键原则

1. **"缺什么补什么"**：混淆后的 JS SDK 会访问大量浏览器属性，每个 `ReferenceError` 或 `TypeError` 都指向一个需要补充的 API
2. **全局注册优先**：通过 `eval`/`Function` 执行的代码只能访问全局作用域，类必须注册到 `ctx.context`
3. **延迟初始化是安全的**：Canvas 只在需要时才初始化（`createElement('canvas')` 触发），避免无关代码的执行开销
4. **toString 很重要**：混淆代码常用 `Object.prototype.toString.call(obj)` 做类型检测，需要正确设置 `Symbol.toStringTag`
5. **属性类型要对**：`PluginArray` 必须继承 `Array.prototype`，`navigator.webdriver` 必须是 `false`（布尔值而非字符串）

### 6.3 文章链接

> 对环境框架的深入分析，推荐阅读：[JavaScript Anti-Anti-Debugging: 抖音弹幕捕获的环境仿真技术深度剖析](https://mp.weixin.qq.com/s/RNqxoslkfRonOKdg5t3fOQ)

---

## 七、最终结果

```
__signature__-> { 'X-Bogus': 'f/4N4cewR5zqR/N9' }
```

成功生成抖音 `X-Bogus` 签名，环境框架可以正常工作。